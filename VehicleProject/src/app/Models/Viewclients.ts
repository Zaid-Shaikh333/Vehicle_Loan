export class Viewclients{
         user_Id ?:number;
         first_name ?:string;
         last_name ?:string;
         status ?:string;
         manufacturer ?:string;
         vehicle_type ?:string;
         vehicle_model ?:string;
         loan_amount ?:number;
         loan_tenure ?:number;
         start_date ?:string;
         end_date ?:string;
         rate_of_interest ?:number;
         monthly_emi ?:number;
    
    }

